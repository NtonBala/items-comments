import React from 'react';

const Form = () => (
    <form>
        <input type='text'/>
        <input
            type='submit'
            value='Add new'
            placeholder='Type name here...'
        />
    </form>
);

export default Form;
